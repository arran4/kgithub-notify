We don't need ui testing just logic unit testing.
All tests must be logic-based, not UI-based. Do not write tests that instantiate widgets and check their properties; instead, extract the logic into helper classes or functions and test those.
Never disable github workflows unless specified.
